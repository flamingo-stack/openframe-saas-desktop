{
  "name": "openframe-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "OpenFrame Desktop — Tauri shell that loads the deployed OpenFrame frontend",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
