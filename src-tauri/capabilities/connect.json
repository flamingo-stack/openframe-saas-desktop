{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "connect",
  "description": "IPC permissions for the connect (host picker) window.",
  "windows": ["connect"],
  "permissions": ["core:default"]
}
