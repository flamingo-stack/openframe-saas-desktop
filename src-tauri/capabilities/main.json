{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "IPC permissions for the windows hosting the bundled frontend (main + child). The bundle is our own code, so it may invoke the native_auth_* commands backing the window.__OPENFRAME_SHELL__.nativeAuth bridge. No window hosts a remote origin: the OAuth login runs in the user's default browser and comes back as an openframe-console:// URL, so no third-party page is ever inside a window this capability covers.",
  "windows": ["main", "child-*"],
  "permissions": ["core:default", "updater:default"]
}
